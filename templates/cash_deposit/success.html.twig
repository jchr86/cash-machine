{% extends 'base.html.twig' %}

{% block title %}Depósito en efectivo{% endblock %}

{% block content %}
    <div class="py-3 mx-auto text-center">
        <h1 class="display-5">Depósito efectuado</h1>
    </div>

    <div class="p-3 mb-2 bg-primary text-white">
        <strong>Saldo actual:</strong>
        ${{ app.user.amount|number_format(2) }}
    </div>

    <div class="row mb-5 justify-content-between">
        <div class="col-md-4">
            <a href="{{ path('homepage') }}" class="btn btn-lg btn-block btn-primary">Menu</a>
        </div>

        <div class="col-md-4">
            <a href="{{ logout_url() }}" class="btn btn-lg btn-block btn-danger">Salir</a>
        </div>
    </div>
{% endblock %}
